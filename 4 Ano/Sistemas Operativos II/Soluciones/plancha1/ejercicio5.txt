Explique el codigo que procesa la instruccion add.

/*
#define SIGN_BIT  0x80000000

sum = registers[instr->rs] + registers[instr->rt];

condicion1 = !((registers[instr->rs] ^ registers[instr->rt]) & SIGN_BIT
condicion2 = registers[instr->rs] ^ sum) & SIGN_BIT

if (condicion1 && condicion2) {
    RaiseException(OVERFLOW_EXCEPTION, 0);
    return;
}

registers[instr->rd] = sum;
*/

Suma los registros indicados en la instruccion y los guarda en el registro indicado, siempre que no haya desbordamiento en cuyo cazo lanza una excepcion.
